package com.ada.genealogyapp.west;

import com.ada.genealogyapp.tree.model.Tree;
import lombok.RequiredArgsConstructor;

import java.util.Map;

//@RequiredArgsConstructor
//public abstract class AbstractImportHandler<T, R> implements ImportHandler<T, R> {
//    private final ImportHandler<T, R> nextHandler;
//
//    @Override
//    public void handle(T input, Tree tree, Map<String, Object> context) {
//        R result = process(input, tree, context);
//        if (nextHandler != null) {
//            nextHandler.handle(input, tree, context);
//        }
//    }
//
//    protected abstract R process(T input, Tree tree, Map<String, Object> context);
//}
